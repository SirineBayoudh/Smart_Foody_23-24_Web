
{% block contenu %}
<style>
  .btn-warning:hover {
        background-color: #388E3C;
    }
    .btn-primary, .btn-warning {
        background-color: #4CAF50;
        color: white;
        border-color: #388E3C;
        animation: slideInUp 1s ease forwards;
    }
</style>
    <div id="commande-container" class="side" style="position: relative; width: 1500px; height: 1500px; border-radius: 8px; overflow: hidden; margin-left: 600px; margin-top: 100px;">
        <div class="container mt-3">
            <div class="row">
                <div class="col-md-12">
                    <h2>Historique des commandes</h2>
                    <table class="table">
                        <thead>
                            <tr>
                                <th>Commande ID</th>
                                <th>Date de commande</th>
                                <th>État de commande</th>
                                <th>Action</th>
                            </tr>
                        </thead>
                        <tbody>
                            {% for commande in historiqueCommandes %}
                                <tr>
                                    <td>{{ commande.id }}</td>
                                    <td>{{ commande.dateCommande|date('Y-m-d') }}</td>
                                    <td>{{ commande.etat }}</td>
                                    <td>
                                            <a href="{{ path('commande_details', {'id': commande.id}) }}" class="btn btn-warning btn-sm">
                                                <img src="{{ asset('images/icons8-eye-100.png') }}" alt="Votre image" width="15" height="15">
                                            </a>
                                        </td> 
                                </tr>
                            {% endfor %}
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
        
        <div class="container mt-3 hidden" id="details-container">
            <div class="row">
                
                      
                        <table class="table table-hover">
                            <thead class="thead-primary">
                                
                            </thead>
                            <tbody id="details-body">
                               
                            </tbody>
                        </table>
                  
            </div>
        </div>
    </div>



{% endblock %}

<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>Export PDF</title>
    <style>
        body {
            font-family: Arial, sans-serif;
        }
        table {
            width: 100%;
            border-collapse: collapse;
            margin-bottom: 20px;
        }
        th, td {
            border: 1px solid #dddddd;
            padding: 8px;
            text-align: left;
        }
        th {
            background-color: #f2f2f2;
        }
    </style>
</head>
<body>
    <h1>Export PDF</h1>
    <table>
        <thead>
            <tr>
                <th>Nom du stock</th>
                <th>Marque</th>
                <th>Quantité</th>
                <th>Nombre vendus</th>
                <th>Coût</th>
                <th>Date</th>
            </tr>
        </thead>
        <tbody>
            {% for item in data %}
                <tr>
                    <td>{{ item.nom }}</td>
                    <td>{{ item.marque }}</td>
                    <td>{{ item.quantite }}</td>
                    <td>{{ item.nbvendu }}</td>
                    <td>{{ item.cout }}</td>
                    <td>{{ item.dateArrivage|date('Y-m-d') }}</td>
                </tr>
            {% endfor %}
        </tbody>
    </table>
</body>
</html>
